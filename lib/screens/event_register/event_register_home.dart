import 'package:flutter/material.dart';

class EventRegisterHomeScreen extends StatelessWidget {
  const EventRegisterHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('EVENT REGISTER HOME SCREEN'),
      ),
    );
  }
}