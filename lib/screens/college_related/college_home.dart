import 'package:flutter/material.dart';

class CollegeHomeScreen extends StatelessWidget {
  const CollegeHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('COLLEGE RELATED HOME SCREEN'),
      ),
    );
  }
}
